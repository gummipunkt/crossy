<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Don't back up auth tokens / server URL — user must re-enter on a new device. -->
    <exclude domain="sharedpref" path="."/>
    <exclude domain="file" path="datastore/"/>
</full-backup-content>
