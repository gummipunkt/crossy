<!DOCTYPE html>
<html>
  <head>
    <title>Crossy <%= content_for(:title) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="apple-touch-icon" sizes="180x180" href="<%= asset_path 'favicon/apple-touch-icon.png' %>">
    <link rel="icon" type="image/png" sizes="32x32" href="<%= asset_path 'favicon/favicon-32x32.png' %>">
    <link rel="icon" type="image/png" sizes="16x16" href="<%= asset_path 'favicon/favicon-16x16.png' %>">
    <link rel="icon" href="<%= asset_path 'favicon/favicon.ico' %>" type="image/x-icon">
    <link rel="manifest" href="<%= asset_path 'favicon/site.webmanifest' %>">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-screen flex flex-col bg-gray-50 text-gray-900">
    <header class="border-b bg-white">
      <div class="max-w-3xl mx-auto px-3 sm:px-4 py-3 sm:py-4 flex flex-wrap items-center justify-between gap-3">
        <div class="flex items-center gap-3">
          <%= link_to root_path, class: "inline-block" do %>
            <%= image_tag "crossy_logo.svg", alt: "Crossy", class: "h-8 w-8" %>
          <% end %>
          <h1 class="text-lg font-semibold"><%= content_for(:title) || "Crossy" %></h1>
        </div>
        <nav class="text-sm flex flex-wrap items-center gap-3 sm:gap-4">
          <% if user_signed_in? %>
            <%= link_to "Composer", new_post_path, class: "text-blue-600 hover:underline" %>
            <%= link_to "Timeline", "/timeline", class: "text-blue-600 hover:underline" %>
            <%= link_to "My Posts", "/my", class: "text-blue-600 hover:underline" %>
            <%= link_to "Networks", provider_accounts_path, class: "text-blue-600 hover:underline" %>
            <% if current_user&.admin? %>
              <%= link_to "Admin", admin_users_path, class: "text-blue-600 hover:underline" %>
            <% end %>
            <span class="text-gray-400 hidden sm:inline">|</span>
            <span class="text-xs text-gray-600 hidden sm:inline"><%= current_user.email %></span>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, form: { class: "inline" }, class: "text-sm text-gray-700 hover:underline" %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "text-blue-600 hover:underline" %>
            <%= link_to "Sign up", new_user_registration_path, class: "text-blue-600 hover:underline" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="flex-1 max-w-3xl mx-auto px-4 py-6">
      <% if notice.present? %>
        <div class="mb-4 rounded-md bg-green-50 text-green-900 px-4 py-3"><%= notice %></div>
      <% end %>
      <% if alert.present? %>
        <div class="mb-4 rounded-md bg-red-50 text-red-900 px-4 py-3"><%= alert %></div>
      <% end %>

      <%= yield %>
    </main>
    <footer class="mt-auto border-t bg-white">
      <div class="max-w-3xl mx-auto px-4 py-4 text-xs text-gray-600 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2">
        <div class="flex items-center gap-2">
          <%= image_tag "crossy_logo.svg", alt: "Crossy", class: "h-5 w-5" %>
          <span>Crossy</span>
        </div>
        <div class="space-x-3">
          <a href="https://interoperable-europe.ec.europa.eu/collection/eupl/eupl-text-eupl-12" target="_blank" rel="noopener" class="hover:underline">License: EUPL v1.2</a>
          <a href="https://github.com/gummipunkt/crossy" target="_blank" rel="noopener" class="hover:underline">GitHub</a>
          <a href="https://www.gummipunkt.eu" target="_blank" rel="noopener" class="hover:underline">made by gummipunkt</a>
        </div>
      </div>
    </footer>
  </body>
</html>
